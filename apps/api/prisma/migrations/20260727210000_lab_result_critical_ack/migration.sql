-- Lab result critical flag + doctor acknowledge

ALTER TABLE "LAB_RESULTS" ADD COLUMN IF NOT EXISTS "CRITICAL_FLAG" VARCHAR(1) DEFAULT 'N';
ALTER TABLE "LAB_RESULTS" ADD COLUMN IF NOT EXISTS "CRITICAL_ACK_AT" TIMESTAMP(3);
ALTER TABLE "LAB_RESULTS" ADD COLUMN IF NOT EXISTS "CRITICAL_ACK_BY" VARCHAR(100);
ALTER TABLE "LAB_RESULTS" ADD COLUMN IF NOT EXISTS "CRITICAL_ACK_BY_ID" INTEGER;

CREATE INDEX IF NOT EXISTS "LAB_RESULTS_CRITICAL_FLAG_idx" ON "LAB_RESULTS"("CRITICAL_FLAG");
